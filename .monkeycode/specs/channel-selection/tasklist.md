# 实现任务列表：支持选择转发到指定频道

## 任务概述

本文档将技术设计方案分解为具体的开发任务，按优先级和依赖关系组织。

---

## 阶段一：基础架构（预计 4-6 小时）

### Task-1.1: 创建频道配置管理模块

**优先级**: P0
**预计工时**: 2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
创建 `src/channel-config.ts` 文件，实现频道配置的加载和管理功能。

**具体步骤**:
1. 定义 `ChannelConfig` 接口
2. 实现 `ChannelConfigManager` 类
3. 实现从环境变量 `TELEGRAM_CHANNELS` 解析配置的逻辑
4. 解析规则：
   - 按逗号分隔
   - 包含冒号：前面是别名，后面是目标
   - 不包含冒号：目标本身当作别名
   - 第一个频道标记为默认频道
5. 实现频道查询方法（按别名）
6. 实现默认频道获取方法
7. 添加配置验证逻辑

**验收标准**:
- [ ] 能够正确解析逗号分隔的频道配置
- [ ] `getAllChannels()` 返回所有频道
- [ ] `getChannelByAlias()` 能通过别名查找频道
- [ ] `getDefaultChannel()` 能返回第一个频道作为默认
- [ ] 支持 @username 和数字 ID 格式
- [ ] 配置错误时有明确的错误提示

**依赖**: 无

---

### Task-1.2: 创建消息缓冲模块

**优先级**: P0
**预计工时**: 2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
创建 `src/message-buffer.ts` 文件，实现消息的临时存储和管理。

**具体步骤**:
1. 定义 `BufferedMessage` 接口
2. 实现 `MessageBuffer` 类
3. 实现消息存储逻辑（使用 KV）
4. 实现消息读取逻辑
5. 实现消息删除逻辑
6. 实现过期消息清理逻辑
7. 添加 TTL 设置

**验收标准**:
- [ ] `bufferMessage()` 能成功存储消息
- [ ] `getBufferedMessage()` 能正确读取消息
- [ ] `removeBufferedMessage()` 能删除消息
- [ ] `cleanupExpiredMessages()` 能清理过期消息
- [ ] 存储的消息包含完整的 TelegramMessage 信息

**依赖**:
- KV Namespace 已配置

---

### Task-1.3: 修改 ForwardHandler 支持动态频道

**优先级**: P0
**预计工时**: 1-2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
修改 `src/forward-handler.ts`，使其支持在转发时动态指定频道 ID。

**具体步骤**:
1. 检查当前 ForwardHandler 的实现
2. 修改构造函数，移除硬编码的 channelId
3. 修改所有转发方法，将 channelId 作为参数传入
4. 保持现有功能不变（向后兼容）
5. 添加类型定义和注释

**验收标准**:
- [ ] ForwardHandler 不再依赖构造函数中的 channelId
- [ ] 所有转发方法都能接受 channelId 参数
- [ ] 现有功能正常工作
- [ ] 单元测试通过

**依赖**:
- Task-1.1（不需要严格依赖，但建议先完成）

---

## 阶段二：命令交互模式（预计 6-8 小时）

### Task-2.1: 创建频道选择器模块

**优先级**: P0
**预计工时**: 2-3 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
创建 `src/channel-selector.ts` 文件，实现频道选择的 UI 和交互逻辑。

**具体步骤**:
1. 定义 `ChannelSelectionState` 接口
2. 实现 `ChannelSelector` 类
3. 实现频道列表展示功能
4. 实现内联按钮生成逻辑
5. 实现频道选择回调处理
6. 添加频道验证逻辑

**验收标准**:
- [ ] `showChannelSelection()` 能显示频道列表
- [ ] `showChannelSelectionInline()` 能生成内联按钮
- [ ] `handleChannelSelection()` 能处理选择结果
- [ ] 支持分页显示（频道数量 > 5 时）

**依赖**:
- Task-1.1（ChannelConfigManager）
- Task-1.2（MessageBuffer）

---

### Task-2.2: 创建命令处理器模块

**优先级**: P0
**预计工时**: 3-4 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
创建 `src/command-handler.ts` 文件，实现 Bot 命令的处理逻辑。

**具体步骤**:
1. 实现 `CommandHandler` 类
2. 实现 `/forward` 命令处理
   - 不带参数：显示频道选择界面
   - 带参数：直接转发到指定频道
3. 实现 `/list` 命令处理（显示所有可用频道）
4. 实现 `/start` 命令处理（显示帮助信息）
5. 实现命令参数解析
6. 添加错误处理

**验收标准**:
- [ ] `/forward` 能正确启动频道选择流程
- [ ] `/forward main` 能直接转发到 main 频道
- [ ] `/list` 能显示所有频道列表
- [ ] `/start` 能显示帮助信息
- [ ] 无效命令和参数有友好的错误提示

**依赖**:
- Task-2.1（ChannelSelector）
- Task-1.1（ChannelConfigManager）
- Task-1.2（MessageBuffer）
- Task-1.3（ForwardHandler）

---

### Task-2.3: 创建消息路由器模块

**优先级**: P0
**预计工时**: 1-2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
创建 `src/message-router.ts` 文件，实现消息的路由和分发。

**具体步骤**:
1. 实现 `MessageRouter` 类
2. 实现命令识别逻辑（以 `/` 开头的消息）
3. 实现普通消息处理逻辑
4. 实现回调查询处理逻辑（用于内联按钮）
5. 集成 CommandHandler
6. 集成现有的 ForwardHandler

**验收标准**:
- [ ] 命令消息能正确路由到 CommandHandler
- [ ] 普通消息能正确路由到现有的处理逻辑
- [ ] 回调查询能正确处理
- [ ] 路由逻辑清晰易维护

**依赖**:
- Task-2.2（CommandHandler）
- Task-1.3（ForwardHandler）

---

### Task-2.4: 修改主入口集成新模块

**优先级**: P0
**预计工时**: 1-2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
修改 `src/index.ts`，集成新的模块并更新 Webhook 处理逻辑。

**具体步骤**:
1. 添加新的环境变量定义
2. 实例化 ChannelConfigManager
3. 实例化 MessageBuffer
4. 实例化 ChannelSelector
5. 实例化 CommandHandler
6. 实例化 MessageRouter
7. 更新 webhook 端点，使用 MessageRouter
8. 更新回调查询处理逻辑
9. 更新健康检查端点

**验收标准**:
- [ ] Webhook 能正确处理所有类型的消息
- [ ] 环境变量配置正确加载
- [ ] 所有模块正确初始化
- [ ] 向后兼容性保持

**依赖**:
- Task-2.3（MessageRouter）
- 所有前置任务

---

## 阶段三：频道选择界面优化（预计 2-3 小时）

### Task-3.1: 优化频道列表显示

**优先级**: P1
**预计工时**: 1 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
优化频道列表的显示效果，提升用户体验。

**具体步骤**:
1. 设计频道列表的显示格式
2. 添加频道图标或标识
3. 标注默认频道
4. 支持频道分组（可选）
5. 添加频道状态指示（已禁用的频道）

**验收标准**:
- [ ] 频道列表清晰易读
- [ ] 默认频道有明确标识
- [ ] 禁用的频道有视觉区分
- [ ] 列表格式美观

**依赖**:
- Task-2.1（ChannelSelector）

---

### Task-3.2: 实现内联按钮回调处理

**优先级**: P1
**预计工时**: 1-2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
完善内联按钮的回调处理逻辑。

**具体步骤**:
1. 定义回调数据格式
2. 实现回调查询解析
3. 处理频道选择回调
4. 更新按钮状态（已转发）
5. 添加错误处理

**验收标准**:
- [ ] 点击按钮能正确转发消息
- [ ] 按钮状态正确更新
- [ ] 回调数据格式规范
- [ ] 错误处理完善

**依赖**:
- Task-2.1（ChannelSelector）
- Task-2.4（主入口）

---

## 阶段四：错误处理和优化（预计 3-4 小时）

### Task-4.1: 完善错误处理

**优先级**: P0
**预计工时**: 2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
为所有模块添加完善的错误处理逻辑。

**具体步骤**:
1. 添加频道权限检查
2. 添加频道有效性验证
3. 添加消息过期处理
4. 添加网络错误重试机制
5. 优化错误提示信息
6. 添加错误日志记录

**验收标准**:
- [ ] 所有错误都有友好的用户提示
- [ ] 错误日志详细准确
- [ ] 网络错误能自动重试
- [ ] 权限错误能明确告知

**依赖**:
- 所有功能模块

---

### Task-4.2: 添加使用帮助和引导

**优先级**: P1
**预计工时**: 1-2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
为新功能添加使用帮助和用户引导。

**具体步骤**:
1. 设计帮助信息内容
2. 实现 `/start` 和 `/help` 命令
3. 添加首次使用引导
4. 创建使用示例
5. 添加错误场景的帮助提示

**验收标准**:
- [ ] `/start` 显示完整的使用说明
- [ ] `/help` 提供详细的命令参考
- [ ] 错误提示包含帮助信息
- [ ] 帮助文档清晰易懂

**依赖**:
- Task-2.2（CommandHandler）

---

## 阶段五：测试和文档（预计 2-3 小时）

### Task-5.1: 编写单元测试

**优先级**: P1
**预计工时**: 2-3 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
为新模块编写单元测试。

**具体步骤**:
1. 为 ChannelConfigManager 编写测试
2. 为 MessageBuffer 编写测试
3. 为 ChannelSelector 编写测试
4. 为 CommandHandler 编写测试
5. 为 MessageRouter 编写测试

**验收标准**:
- [ ] 核心逻辑有完整的测试覆盖
- [ ] 测试用例覆盖正常流程和异常流程
- [ ] 所有测试通过
- [ ] 代码覆盖率 > 80%

**依赖**:
- 所有功能模块

---

### Task-5.2: 编写集成测试和手动测试

**优先级**: P1
**预计工时**: 1-2 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
编写集成测试并执行手动测试。

**具体步骤**:
1. 编写端到端集成测试
2. 在真实环境中测试所有功能
3. 测试错误场景
4. 记录测试结果
5. 修复发现的问题

**验收标准**:
- [ ] 所有功能在真实环境中正常工作
- [ ] 错误场景得到正确处理
- [ ] 测试记录完整
- [ ] 发现的问题全部修复

**依赖**:
- Task-5.1（单元测试）
- 所有功能模块

---

### Task-5.3: 更新 README 和文档

**优先级**: P2
**预计工时**: 1 小时
**负责人**: -
**状态**: 待开始

**任务描述**:
更新项目文档，说明新功能的使用方法。

**具体步骤**:
1. 更新 README.md
2. 添加环境变量配置说明
3. 添加使用示例
4. 添加故障排查指南
5. 更新部署说明

**验收标准**:
- [ ] README 包含完整的功能说明
- [ ] 环境变量配置文档清晰
- [ ] 使用示例可执行
- [ ] 文档与实际功能一致

**依赖**:
- 所有功能完成

---

## 部署和上线（预计 1-2 小时）

### Task-6.1: 配置 Cloudflare Workers 环境

**优先级**: P0
**预计工时**: 30 分钟
**负责人**: -
**状态**: 待开始

**任务描述**:
配置 Cloudflare Workers 的运行环境。

**具体步骤**:
1. 创建 KV Namespace
2. 绑定 KV Namespace 到 Worker
3. 配置环境变量
4. 测试配置是否生效

**验收标准**:
- [ ] KV Namespace 创建成功
- [ ] KV 绑定正确
- [ ] 环境变量配置正确
- [ ] Worker 能正常启动

**依赖**:
- 所有代码完成

---

### Task-6.2: 部署和验证

**优先级**: P0
**预计工时**: 30-60 分钟
**负责人**: -
**状态**: 待开始

**任务描述**:
部署到生产环境并验证功能。

**具体步骤**:
1. 使用 Wrangler 部署 Worker
2. 设置 Webhook
3. 测试基本功能
4. 测试新功能
5. 监控日志和错误

**验收标准**:
- [ ] Worker 部署成功
- [ ] Webhook 设置成功
- [ ] 所有功能正常工作
- [ ] 日志正常输出

**依赖**:
- Task-6.1（环境配置）

---

## 任务依赖关系图

```
阶段一：
Task-1.1 ─────┐
             ├─▶ Task-1.3 ──┐
Task-1.2 ─────┘              │
                            ▼
阶段二：                     │
Task-2.1 ──▶ Task-2.2 ──────┤
             │              │
             ▼              │
          Task-2.3 ────────┘
             │
             ▼
          Task-2.4

阶段三：
Task-3.1 ──▶ Task-3.2
             │
             ▼
阶段四：
Task-4.1 ──▶ Task-4.2

阶段五：
Task-5.1 ──▶ Task-5.2 ──▶ Task-5.3

阶段六：
Task-6.1 ──▶ Task-6.2
```

---

## 优先级说明

- **P0**: 必须完成的核心功能，阻塞后续开发
- **P1**: 重要功能，建议在第一阶段完成后尽快实现
- **P2**: 增强功能，可根据实际情况安排

---

## 总工时估算

| 阶段 | 预计工时 |
|------|---------|
| 阶段一：基础架构 | 4-6 小时 |
| 阶段二：命令交互模式 | 6-8 小时 |
| 阶段三：频道选择界面优化 | 2-3 小时 |
| 阶段四：错误处理和优化 | 3-4 小时 |
| 阶段五：测试和文档 | 2-3 小时 |
| 阶段六：部署和上线 | 1-2 小时 |
| **总计** | **18-26 小时** |

---

## 风险和注意事项

1. **KV 存储限制**: 注意 KV 的读写限制和成本
2. **环境变量长度**: 如果频道配置很大，可能需要改用数据库
3. **向后兼容性**: 确保现有功能不受影响
4. **测试覆盖**: 重点测试边缘情况和错误场景
5. **用户体验**: 频道选择流程要简单直观

---

## 下一步行动

1. 审核并确认需求和设计方案
2. 根据实际情况调整任务列表
3. 分配任务给开发人员
4. 开始实施阶段一的任务